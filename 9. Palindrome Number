bool isPalindrome(int x){
if (x < 0) return false;
long int result = 0, q, rem;
q = x;
while(q != 0){
    rem = q%10;
    result = result*10 + rem;
    q = q/10;
}
if(result == x)
    return true; 
else
    return false;
}
